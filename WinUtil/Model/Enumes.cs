namespace WinUtil.Model
{
    /// <summary>
    /// enum for file size leagues
    /// </summary>
    public enum FileSize
    {
        Bytes,
        KiloBytes,
        MegaBytes,
        GigaBytes,
        PetaBytes
    }
}
